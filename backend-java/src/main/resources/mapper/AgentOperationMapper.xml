<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itxiang.evidence.mapper.AgentOperationMapper">

    <resultMap id="AgentOperationResultMap" type="com.itxiang.evidence.entity.AgentOperation">
        <id property="id" column="id"/>
        <result property="taskId" column="task_id"/>
        <result property="reviewId" column="review_id"/>
        <result property="userId" column="user_id"/>
        <result property="operationType" column="operation_type"/>
        <result property="resourceType" column="resource_type"/>
        <result property="resourceId" column="resource_id"/>
        <result property="status" column="status"/>
        <result property="beforeSnapshotRef" column="before_snapshot_ref"/>
        <result property="afterSnapshotRef" column="after_snapshot_ref"/>
        <result property="idempotencyKey" column="idempotency_key"/>
        <result property="undoDeadline" column="undo_deadline"/>
        <result property="auditEventId" column="audit_event_id"/>
        <result property="errorCode" column="error_code"/>
        <result property="errorMessage" column="error_message"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.itxiang.evidence.entity.AgentOperation">
        INSERT INTO agent_operation (
            id,
            task_id,
            review_id,
            user_id,
            operation_type,
            resource_type,
            resource_id,
            status,
            before_snapshot_ref,
            after_snapshot_ref,
            idempotency_key,
            undo_deadline,
            audit_event_id,
            error_code,
            error_message
        )
        VALUES (
            #{id},
            #{taskId},
            #{reviewId},
            #{userId},
            #{operationType},
            #{resourceType},
            #{resourceId},
            #{status},
            #{beforeSnapshotRef},
            #{afterSnapshotRef},
            #{idempotencyKey},
            #{undoDeadline},
            #{auditEventId},
            #{errorCode},
            #{errorMessage}
        )
    </insert>

    <select id="findById" resultMap="AgentOperationResultMap">
        SELECT *
        FROM agent_operation
        WHERE id = #{id}
    </select>

    <select id="findByTaskId" resultMap="AgentOperationResultMap">
        SELECT *
        FROM agent_operation
        WHERE task_id = #{taskId}
        ORDER BY created_at ASC, id ASC
    </select>

    <select id="findByIdempotency" resultMap="AgentOperationResultMap">
        SELECT *
        FROM agent_operation
        WHERE user_id = #{userId}
          AND operation_type = #{operationType}
          AND resource_type = #{resourceType}
          AND resource_id = #{resourceId}
          AND idempotency_key = #{idempotencyKey}
    </select>

    <update id="updateResult" parameterType="com.itxiang.evidence.entity.AgentOperation">
        UPDATE agent_operation
        SET status = #{status},
            before_snapshot_ref = #{beforeSnapshotRef},
            after_snapshot_ref = #{afterSnapshotRef},
            undo_deadline = #{undoDeadline},
            audit_event_id = #{auditEventId},
            error_code = #{errorCode},
            error_message = #{errorMessage},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>
</mapper>
