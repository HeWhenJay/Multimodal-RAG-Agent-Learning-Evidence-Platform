<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itxiang.evidence.mapper.AgentTaskMapper">

    <resultMap id="AgentTaskResultMap" type="com.itxiang.evidence.entity.AgentTask">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="taskType" column="task_type"/>
        <result property="status" column="status"/>
        <result property="title" column="title"/>
        <result property="inputJson" column="input_json"/>
        <result property="planJson" column="plan_json"/>
        <result property="draftJson" column="draft_json"/>
        <result property="finalJson" column="final_json"/>
        <result property="pythonThreadId" column="python_thread_id"/>
        <result property="errorCode" column="error_code"/>
        <result property="errorMessage" column="error_message"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.itxiang.evidence.entity.AgentTask">
        INSERT INTO agent_task (
            id,
            user_id,
            task_type,
            status,
            title,
            input_json,
            plan_json,
            draft_json,
            final_json,
            python_thread_id,
            error_code,
            error_message
        )
        VALUES (
            #{id},
            #{userId},
            #{taskType},
            #{status},
            #{title},
            #{inputJson},
            #{planJson},
            #{draftJson},
            #{finalJson},
            #{pythonThreadId},
            #{errorCode},
            #{errorMessage}
        )
    </insert>

    <select id="findById" resultMap="AgentTaskResultMap">
        SELECT
            id,
            user_id,
            task_type,
            status,
            title,
            input_json,
            plan_json,
            draft_json,
            final_json,
            python_thread_id,
            error_code,
            error_message,
            created_at,
            updated_at
        FROM agent_task
        WHERE id = #{id}
    </select>

    <select id="findByIdAndUserId" resultMap="AgentTaskResultMap">
        SELECT
            id,
            user_id,
            task_type,
            status,
            title,
            input_json,
            plan_json,
            draft_json,
            final_json,
            python_thread_id,
            error_code,
            error_message,
            created_at,
            updated_at
        FROM agent_task
        WHERE id = #{id}
          AND user_id = #{userId}
    </select>

    <update id="updateFromEvent" parameterType="com.itxiang.evidence.entity.AgentTask">
        UPDATE agent_task
        SET status = #{status},
            plan_json = #{planJson},
            draft_json = #{draftJson},
            final_json = #{finalJson},
            python_thread_id = #{pythonThreadId},
            error_code = #{errorCode},
            error_message = #{errorMessage},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>
</mapper>
