<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itxiang.evidence.mapper.LogErrorMapper">

    <resultMap id="LogErrorResultMap" type="com.itxiang.evidence.entity.LogError">
        <id property="id" column="id"/>
        <result property="traceId" column="trace_id"/>
        <result property="sessionId" column="session_id"/>
        <result property="userId" column="user_id"/>
        <result property="source" column="source"/>
        <result property="domain" column="domain"/>
        <result property="severity" column="severity"/>
        <result property="module" column="module"/>
        <result property="stage" column="stage"/>
        <result property="action" column="action"/>
        <result property="errorType" column="error_type"/>
        <result property="errorCode" column="error_code"/>
        <result property="message" column="message"/>
        <result property="stackTrace" column="stack_trace"/>
        <result property="fingerprint" column="fingerprint"/>
        <result property="route" column="route"/>
        <result property="httpMethod" column="http_method"/>
        <result property="requestPath" column="request_path"/>
        <result property="statusCode" column="status_code"/>
        <result property="durationMs" column="duration_ms"/>
        <result property="materialId" column="material_id"/>
        <result property="documentId" column="document_id"/>
        <result property="parser" column="parser"/>
        <result property="clientTime" column="client_time"/>
        <result property="serverTime" column="server_time"/>
        <result property="contextJson" column="context_json"/>
        <result property="firstSeenAt" column="first_seen_at"/>
        <result property="lastSeenAt" column="last_seen_at"/>
        <result property="occurrenceCount" column="occurrence_count"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.itxiang.evidence.entity.LogError"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO log_error (
            trace_id,
            session_id,
            user_id,
            source,
            domain,
            severity,
            module,
            stage,
            action,
            error_type,
            error_code,
            message,
            stack_trace,
            fingerprint,
            route,
            http_method,
            request_path,
            status_code,
            duration_ms,
            material_id,
            document_id,
            parser,
            client_time,
            server_time,
            context_json,
            status
        )
        VALUES (
            #{traceId},
            #{sessionId},
            #{userId},
            #{source},
            #{domain},
            #{severity},
            #{module},
            #{stage},
            #{action},
            #{errorType},
            #{errorCode},
            #{message},
            #{stackTrace},
            #{fingerprint},
            #{route},
            #{httpMethod},
            #{requestPath},
            #{statusCode},
            #{durationMs},
            #{materialId},
            #{documentId},
            #{parser},
            #{clientTime},
            #{serverTime},
            #{contextJson},
            #{status}
        )
    </insert>

    <update id="increaseOccurrence">
        UPDATE log_error
        SET occurrence_count = occurrence_count + 1,
            last_seen_at = #{lastSeenAt},
            updated_at = CURRENT_TIMESTAMP
        WHERE fingerprint = #{fingerprint}
    </update>

    <select id="findByFingerprint" resultMap="LogErrorResultMap">
        SELECT
            id,
            trace_id,
            session_id,
            user_id,
            source,
            domain,
            severity,
            module,
            stage,
            action,
            error_type,
            error_code,
            message,
            stack_trace,
            fingerprint,
            route,
            http_method,
            request_path,
            status_code,
            duration_ms,
            material_id,
            document_id,
            parser,
            client_time,
            server_time,
            context_json,
            first_seen_at,
            last_seen_at,
            occurrence_count,
            status,
            created_at,
            updated_at
        FROM log_error
        WHERE fingerprint = #{fingerprint}
    </select>

    <select id="findRecent" resultMap="LogErrorResultMap">
        SELECT
            id,
            trace_id,
            session_id,
            user_id,
            source,
            domain,
            severity,
            module,
            stage,
            action,
            error_type,
            error_code,
            message,
            stack_trace,
            fingerprint,
            route,
            http_method,
            request_path,
            status_code,
            duration_ms,
            material_id,
            document_id,
            parser,
            client_time,
            server_time,
            context_json,
            first_seen_at,
            last_seen_at,
            occurrence_count,
            status,
            created_at,
            updated_at
        FROM log_error
        ORDER BY last_seen_at DESC, id DESC
        LIMIT #{limit}
    </select>

    <select id="countSince" resultType="long">
        SELECT COUNT(1)
        FROM log_error
        WHERE created_at &gt;= #{startTime}
    </select>

    <select id="countOpenSince" resultType="long">
        SELECT COUNT(1)
        FROM log_error
        WHERE created_at &gt;= #{startTime}
          AND status = 'OPEN'
    </select>

    <select id="countBySourceSince" resultType="long">
        SELECT COUNT(1)
        FROM log_error
        WHERE created_at &gt;= #{startTime}
          AND source = #{source}
    </select>
</mapper>
